.DS_Store
.vscode/
.claude/

# Raw screenshot sources (kept locally; optimized WebP lives in screenshots/)
busbuddy_images/

# Reel build artifacts (kept locally; deployed copy lives in media/)
promo/

# GymOS raw assets (kept locally; optimized in screenshots/gymos + media/)
# Leading slash anchors to repo root so it does NOT match screenshots/gymos/
/gymos/
